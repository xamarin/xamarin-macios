## the enum is declared before the EAWiFiUnconfiguredAccessory type, which is `NS_CLASS_AVAILABLE(NA, 8_0)`


## the enums are declared before the EAWiFiUnconfiguredAccessoryBrowser type, which is `NS_CLASS_AVAILABLE(NA, 8_0)`


## the delegate is declared before the EAWiFiUnconfiguredAccessoryBrowser type, which is `NS_CLASS_AVAILABLE(NA, 8_0)`

## is says __MAC_NA but its already bound and intro says it's present (mistake?)
